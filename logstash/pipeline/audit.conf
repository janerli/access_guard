input {
  kafka {
    bootstrap_servers => "${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}"
    topics => ["audit.events"]
    group_id => "logstash-audit"
    consumer_threads => 2
    auto_offset_reset => "earliest"
    codec => "json"
    decorate_events => true
  }
}

filter {
  # The Kafka message is a KafkaEvent envelope:
  # { event_type, producer, correlation_id, payload: { event_id, timestamp, actor_username, ... } }
  # We need to flatten payload fields to the document root FIRST,
  # then set @timestamp from the top-level timestamp field.

  if [payload] {
    mutate {
      add_field => {
        "event_id"        => "%{[payload][event_id]}"
        "audit_log_id"    => "%{[payload][audit_log_id]}"
        "timestamp"       => "%{[payload][timestamp]}"
        "actor_id"        => "%{[payload][actor_id]}"
        "actor_username"  => "%{[payload][actor_username]}"
        "target_type"     => "%{[payload][target_type]}"
        "target_id"       => "%{[payload][target_id]}"
        "operation"       => "%{[payload][operation]}"
        "module"          => "%{[payload][module]}"
        "result"          => "%{[payload][result]}"
        "ip_address"      => "%{[payload][ip_address]}"
        "correlation_id"  => "%{[payload][correlation_id]}"
        "department_code" => "%{[payload][department_code]}"
        "position_code"   => "%{[payload][position_code]}"
      }
    }

    # Move nested details object to top level without stringifying
    if [payload][details] {
      mutate {
        rename => { "[payload][details]" => "details" }
      }
    }

    mutate { remove_field => ["payload"] }
  }

  # Set @timestamp from the event's actual timestamp (now at top level).
  # Must run AFTER the payload flatten above.
  if [timestamp] and [timestamp] != "%{[payload][timestamp]}" {
    date {
      match => ["timestamp", "ISO8601"]
      target => "@timestamp"
    }
  }

  # audit_log_id comes in as a string from add_field — convert to integer
  if [audit_log_id] {
    mutate {
      convert => { "audit_log_id" => "integer" }
    }
  }

  mutate { remove_field => ["@version"] }
}

output {
  if "app-log" not in [tags] {
    elasticsearch {
      hosts => ["${ELASTICSEARCH_URL:http://elasticsearch:9200}"]
      index => "audit-events-%{+YYYY.MM}"
      document_id => "%{event_id}"
      action => "index"
    }
  }
}
